import { PopularBtn } from './popular-btn';

describe('PopularBtn', () => {
  it('should create an instance', () => {
    const directive = new PopularBtn();
    expect(directive).toBeTruthy();
  });
});
