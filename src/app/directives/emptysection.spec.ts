import { Emptysection } from './emptysection';

describe('Emptysection', () => {
  it('should create an instance', () => {
    const directive = new Emptysection();
    expect(directive).toBeTruthy();
  });
});
